class Solution {
    public int maxProduct(int[] nums) {
        int max=nums[0];
        for(int i=0;i<nums.length;i++){
            int c=1;
            for(int j=i;j<nums.length;j++){
                c=c*nums[j];
                if(c>max){
                    max=c;
                }
            }
        }
        return max;
    }
}
